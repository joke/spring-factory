plugins {
    id 'java'
}

dependencies {
    annotationProcessor project(':spring-factory')

    compileOnly project(':spring-factory-annotations')

    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.6.2'
}
